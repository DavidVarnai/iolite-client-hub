/**
 * Admin domain barrel — admin types and configuration.
 */
export type {
  IntegrationConfig,
  AdminUser,
  RoleDefinition,
  AdminTemplate,
  TaxonomyGroup,
  TaxonomyItem,
  AiModuleControl,
  SystemSettings,
} from '@/types/admin';

export { isAdminUser } from '@/types/admin';
